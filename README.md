Scan multiple directories and compare all images for duplicates. Keep only the highest resoulition version of the image and delete duplicates. Uses AI model to detect contect. 
Needs GPU and additional model download will start after running script first time.

